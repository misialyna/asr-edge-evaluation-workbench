# Recommendation weights used by the analysis layer.
# Weights are normalized to sum to 1 internally; only the relative
# ratios matter. The defaults emphasize accuracy (50%), real-time
# performance (30%), and memory (20%). Edit this file, not the code,
# to change the recommendation.

wer: 0.5
rtfx: 0.3
ram: 0.2
